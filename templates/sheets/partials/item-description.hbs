<div class="item-content">
  <div class="field-name">{{localize "CONAN.LABEL.description"}}</div>
  {{#if unlocked}}
    {{formInput systemFields.description enriched=descriptionHTML value=system.description}}
  {{else}}
    {{{descriptionHTML}}}
  {{/if}}
</div>