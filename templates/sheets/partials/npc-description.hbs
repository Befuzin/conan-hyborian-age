<section class="tab description {{tab.cssClass}}" data-group="primary" data-tab="description">
  <h2>{{localize "CONAN.LABEL.background"}}</h2>
  {{#if unlocked}}
    {{formInput systemFields.description enriched=descriptionHTML value=system.description}}
  {{else}}
    {{{descriptionHTML}}}
  {{/if}}
</section>